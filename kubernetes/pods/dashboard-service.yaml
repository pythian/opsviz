apiVersion: v1beta3
kind: Service
metadata:
  name: dashboard
  labels:
    name: dashboard
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: povs
    component: dashboard
