apiVersion: v1beta3
kind: Service
metadata:
  labels:
    name: dashboard
  name: dashboard-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: povs
    component: dashboard