kind: ReplicationController
apiVersion: v1beta3
metadata:
  labels:
    app: povs
    name: dashboard
    role: nginx
  name: dashboard-nginx
spec:
  replicas: 1
  selector:
    name: dashboard
    role: nginx
  template:
    metadata:
      labels:
        name: dashboard
        role: nginx
    spec:
        containers:
          - name: dashboard-nginx
            image: alexlovelltroy/opsviz-dashboard
            ports:
              - name: http
                containerPort: 8080